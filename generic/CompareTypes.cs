
namespace generic
{
    internal class CompareTypes
    {
        public bool compare<T>(T x , T y)
        {
            return x.Equals(y);
        }

        //strings
        //public bool compare(string x, string y)
        //{
        //    return x == y;
        //}

        //public bool compare(double x, double y)
        //{
        //    return x == y;
        //}

        //Code Repetition- non clean code
    }
}
